apiVersion: v1
kind: Service
metadata:
  name: hify-backend
  namespace: hify
  labels:
    app: hify-backend
spec:
  type: ClusterIP
  selector:
    app: hify-backend
  ports:
    - name: http
      port: 8090
      targetPort: 8090
      protocol: TCP
